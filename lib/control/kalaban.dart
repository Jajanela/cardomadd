import 'package:flutter/material.dart';

class kalaban extends StatelessWidget {
  const kalaban({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 80,
      height: 80,
      child: Image.asset('lib/images/kapre.png'),
    );
  }
}
